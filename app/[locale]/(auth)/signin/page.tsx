import { SignInContainer } from "@/features/auth";

export const metadata = {
  title: "Sign In | POS",
  description: "Sign in to manage your company/business with qmona.com",
};

const SignInPage = () => {
  return <SignInContainer />;
};

export default SignInPage;

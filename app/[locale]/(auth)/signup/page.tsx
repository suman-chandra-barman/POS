import { SignUpContainer } from "@/features/auth";

export const metadata = {
  title: "Sign Up | POS",
  description: "Sign up to manage your company/business with qmona.com",
};

const SignUpPage = () => {
  return <SignUpContainer />;
};

export default SignUpPage;
